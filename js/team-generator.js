document.addEventListener('DOMContentLoaded',()=>{const input=document.querySelector('#participants'),teams=document.querySelector('#teamCount'),btn=document.querySelector('#makeTeams'),out=document.querySelector('#teamResults');if(!btn)return;btn.addEventListener('click',()=>{const names=input.value.split(/\r?\n/).map(s=>s.trim()).filter(Boolean);const n=Math.max(2,Math.min(8,Number(teams.value)));if(names.length<n){out.innerHTML='<div class="notice">참가자 수가 팀 수보다 적습니다.</div>';return}for(let i=names.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[names[i],names[j]]=[names[j],names[i]]}out.innerHTML='';const boxes=Array.from({length:n},(_,i)=>{const d=document.createElement('div');d.className='team-card';d.innerHTML=`<h3>${String.fromCharCode(65+i)}팀</h3>`;out.appendChild(d);return d});names.forEach((name,i)=>{setTimeout(()=>{const div=document.createElement('div');div.className='member';div.textContent=name;div.style.animationDelay='0s';boxes[i%n].appendChild(div)},i*90)})});});
